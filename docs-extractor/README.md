Extracts command options from the `yt-dlp --help` output.

## Usage

```sh
docs-extractor (/path/to/yt-dlp)
```

Run the program, an output should be found at `output.rs`. You must have `yt-dlp` installed on your system.
